# Xentral MCP Server Dependencies

# Web framework and HTTP
Flask>=3.0

# Production WSGI server (do not use the Flask dev server for exposed deployments)
gunicorn>=22.0

# Environment configuration
python-dotenv>=1.0

# HTTP client
httpx>=0.27

# MCP SDK (used by the legacy stdio server.py)
mcp>=0.9

# Table formatting
tabulate>=0.9

# JSON and data handling (built-in to Python 3.8+)
# json (built-in)
# dataclasses (built-in to Python 3.7+)
# typing (built-in to Python 3.5+)
# pathlib (built-in to Python 3.4+)
# re (built-in)
# logging (built-in)
# os (built-in)
# sys (built-in)
# datetime (built-in)

# Development and testing dependencies (optional)
# pytest==7.4.2
# pytest-flask==1.2.0
# requests==2.31.0
